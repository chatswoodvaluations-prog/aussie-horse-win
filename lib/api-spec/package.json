{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && find ../../lib/api-zod/src/generated -name '*.ts' -exec sed -i 's/zod\\.int()/zod.number().int()/g' {} + && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.23.0"
  }
}
